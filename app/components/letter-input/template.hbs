{{#each alphabet as |letter|}}
  <span class={{if letter.selected 'c-letter-input__letter c-letter-input__letter--selected' 'c-letter-input__letter'}} {{action "handleLetterClick" letter.char}}>{{letter.char}}</span>
{{/each}}
